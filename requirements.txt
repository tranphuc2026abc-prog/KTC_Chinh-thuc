# --- Core Application ---
streamlit>=1.40.0
watchdog>=3.0.0
nest_asyncio
numpy<2.0.0

# --- Multimodal (Audio & Image) ---
# QUAN TRỌNG: Phải dùng SpeechRecognition bản mới nhất để fix lỗi 'aifc' trên Python 3.13
SpeechRecognition>=3.11.0
gTTS>=2.5.0
Pillow>=10.0.0
# Thư viện ghi âm cho Streamlit
streamlit-mic-recorder>=0.0.8

# --- TTS (Text-to-Speech) ---
# Thư viện đọc giọng Nam (Microsoft Edge)
edge-tts>=6.1.9

# --- LLM & AI Clients ---
groq>=0.4.0
openai-whisper>=20231117

# --- RAG & LangChain Ecosystem ---
langchain>=0.1.0
langchain-community>=0.0.20
langchain-huggingface>=0.0.1
langchain-text-splitters>=0.0.1
langchain-groq

# --- Vector Database & Retrieval ---
faiss-cpu>=1.7.4
flashrank>=0.2.0
rank_bm25>=0.2.2

# --- Document Processing (PDF) ---
pypdf
pymupdf
llama-parse

# --- Deep Learning Utilities ---
sentence-transformers>=2.2.2
transformers>=4.30.0

# --- System Utils (Fix lỗi thiếu thư viện hệ thống) ---
packaging
requests